#pragma once
#include <string_view>

namespace helpers::consts {
    // Prefix shown in console + HUD toasts for human-friendly tagging.
    inline constexpr std::string_view kConsoleTag = "[racemenu morph fixer] ";
}